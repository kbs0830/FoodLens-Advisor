# FoodLens-Advisor — 完整工作流程

本文檔說明從「取得專案」到「本地測試、提交、推上遠端、部署」的完整步驟，包含常見偵錯與建議。請依環境（Windows）與專案目錄執行。

---

## 1. 取得專案
- 從遠端 clone 或複製整個專案資料夾到本機：

```powershell
# 使用 HTTPS
git clone https://github.com/username/repo.git
# 或 SSH
git clone git@github.com:username/repo.git
```

（略）

(完整備份已存於此檔案。)
